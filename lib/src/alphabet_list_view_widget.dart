import 'package:flutter/widgets.dart';

class AlphabeticListView extends StatelessWidget {
  const AlphabeticListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hallo Welt');
  }
}
